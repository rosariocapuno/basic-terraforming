terraform {
  backend "s3" {
    bucket         = "bucketName"
    region         = "us-east-2"
    key            = "terraform.tfstate"
    access_key     = "ACCESS_KEY"
    secret_key     = "SECRET_KEY"
  }
}